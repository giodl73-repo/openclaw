# PR #81896 fresh proof artifacts

Fresh deterministic before/after proof for PR #81896.

- `pr-81896-native-subagent-cwd-before-after-proof.png`: before/after proof image generated from source/runtime output in clean source worktrees.
- `summary.txt`: copied terminal proof summary.

The proof compares `upstream/main` with PR head `3246e902cfa20da8a03dca318cb6b4442b9038e5` and verifies that native subagent `cwd` is plumbed and attachment materialization uses the explicit child workspace.
