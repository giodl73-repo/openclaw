# PR #82625 fresh proof artifacts

Fresh deterministic before/after proof for PR #82625.

- `pr-82625-telegram-partial-final-dedupe-before-after-proof.png`: before/after proof image generated from real `buildReplyPayloads()` output.
- `summary.txt`: copied terminal proof summary.

The proof compares `upstream/main` with PR head `15d9d500cd45b8b1db50d90d13fdd62c17cbdb76` using `previewStreamedText` plus final duplicate text, unrelated short text, and media-caption payloads.
