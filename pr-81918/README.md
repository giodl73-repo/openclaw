# PR #81918 fresh proof artifacts

Fresh deterministic before/after proof for PR #81918.

- `pr-81918-bootstrap-truncation-warning-before-after-proof.png`: before/after proof image generated from real resolver output in clean source worktrees.
- `summary.txt`: copied terminal proof summary.

The proof compares `upstream/main` with PR head `92e48bc6889a7251ba4acc2fc67a6cbcf319ff2b` and verifies that bootstrap prompt truncation warnings default to `always` while explicit modes remain honored.
