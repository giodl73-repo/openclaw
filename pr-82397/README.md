# PR #82397 fresh proof artifacts

Fresh deterministic before/after proof for PR #82397.

- `pr-82397-workspaceonly-skill-read-before-after-proof.png`: before/after proof image generated from real `createOpenClawCodingTools()` output.
- `summary.txt`: copied terminal proof summary.

The proof compares `upstream/main` with PR head `2018a9360c662bae7e24f15a470144fe91f451e2` using `tools.fs.workspaceOnly` and a runtime `SkillSnapshot` resolved skill root.
